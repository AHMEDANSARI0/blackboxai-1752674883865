import React from 'react';

const RTL = () => (
  <div className="rtl-page">
    <h2>RTL (Right-to-Left)</h2>
    <p>This page demonstrates right-to-left layout support for languages like Arabic, Hebrew, and Persian. The interface adapts to provide proper text direction and layout mirroring.</p>
  </div>
);

export default RTL;
